# tools/ — Ferramentas do Workspace
> Scripts pequenos para verificar a organização e compilar o artigo.

- `workspace_check.py`: confirma que a estrutura mínima existe e que não há outro Git dentro de `article/`.
- `compile_article.py`: compila `article/samplepaper.tex` em `outputs/article/`.
